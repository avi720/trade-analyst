# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Google Identity Services — the OAuth *Web* client ID (public, not a secret). Must be listed
# under Supabase > Auth > Providers > Google > Client IDs for the same Supabase project, and
# its Authorized JavaScript origins must include this site's origin.
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com

# NOTE: DATABASE_URL / DIRECT_URL were removed in 2026-08. They were Prisma-era
# connection strings; nothing reads them since the switch to the Supabase JS SDK.
# Do not re-add them — a raw Postgres URL embeds the DB password, and a stray copy
# of one is what leaked into git history in April 2026. If you need direct psql
# access, pull the string from the dashboard when you need it and don't persist it.

# Encryption key for IBKR Flex Token (32-byte hex)
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
FLEX_TOKEN_ENCRYPTION_KEY=

# External APIs
MASSIVE_API_KEY=
GEMINI_API_KEY=

# Cron authentication — Bearer token expected by /api/cron/* endpoints.
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CRON_SECRET=

# Canonical app URL — used server-side by getBaseUrl() to build redirect/callback URLs.
# Set in Vercel dashboard (e.g. https://your-app.vercel.app); omit locally.
SITE_URL=https://your-app.vercel.app

# AI Excel import — OPTIONAL near-instant worker trigger. When both are set, the
# upload route fires a GitHub repository_dispatch so the worker runs within
# seconds; otherwise the '*/30 * * * *' schedule in ai-import-worker.yml picks
# the job up. Token = fine-grained PAT with "Contents: Read and write" scoped to
# the repo (dispatch requires write). Repo = "owner/repo". Both server-only.
# Omit to rely on the schedule alone.
AI_IMPORT_DISPATCH_TOKEN=
AI_IMPORT_DISPATCH_REPO=

# Geo gate — restricts the app + API to an allow-list of countries. The public marketing
# pages (/, /pricing, /terms, /privacy, the 3 landing pages, /og) and the Lemon Squeezy
# webhook stay reachable worldwide so SEO, link previews and billing keep working.
# See docs/in-progress/AUTH-HARDENING-GEO-GATE.md. All three are server-only.
#
# OFF unless GEO_GATE_ENABLED is exactly 'true'. The gate also fails OPEN when the host
# sends no x-vercel-ip-country header (localhost / next dev) or when it resolves to 'XX'.
GEO_GATE_ENABLED=
# Comma-separated ISO-3166-1 alpha-2 codes. Defaults to IL when unset.
GEO_ALLOWED_COUNTRIES=IL
# Escape hatch for travelling abroad: loading any URL with ?geo_bypass=<this value> sets a
# 90-day cookie exempting that browser. Unset = escape hatch disabled. Rotate to revoke all.
# Generate: node -e "console.log(require('crypto').randomBytes(24).toString('hex'))"
GEO_BYPASS_SECRET=

# Sentry — error tracking. DSN is browser-safe (public).
# Get from Sentry → Settings → Projects → Client Keys (DSN).
NEXT_PUBLIC_SENTRY_DSN=

# Sentry auth token — required only for source-map upload during build.
# Get from Sentry → Settings → Auth Tokens (scope: project:releases, org:read).
SENTRY_AUTH_TOKEN=

# PostHog — analytics + signup funnel. Project API key is browser-safe (public).
# Get from PostHog → Settings → Project → Project API key.
NEXT_PUBLIC_POSTHOG_KEY=
# Default to US cloud; switch to https://eu.i.posthog.com if project is on EU cloud.
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com

# Lemon Squeezy — billing (merchant of record).
# Get from https://app.lemonsqueezy.com → Settings → API → API Keys.
LEMONSQUEEZY_API_KEY=
# Store ID: visible in dashboard URL (app.lemonsqueezy.com/stores/<id>).
LEMONSQUEEZY_STORE_ID=
# Variant IDs for the Pro product. Get from Products → Pro → Variants.
LEMONSQUEEZY_VARIANT_ID_MONTHLY=
LEMONSQUEEZY_VARIANT_ID_ANNUAL=
# Webhook signing secret: Settings → Webhooks → Signing secret.
LEMONSQUEEZY_WEBHOOK_SECRET=
# Launch promo discount CODES (optional — omit after promo ends). Note: use the
# discount CODE (e.g. "IYNDCZNQ"), not the numeric ID — LS checkout API expects
# checkout_data.discount_code. Create in Discounts → New Discount. Monthly:
# $5 off, 3 repeating months. Annual: $50 off, once.
LEMONSQUEEZY_DISCOUNT_CODE_LAUNCH_MONTHLY=
LEMONSQUEEZY_DISCOUNT_CODE_LAUNCH_ANNUAL=
